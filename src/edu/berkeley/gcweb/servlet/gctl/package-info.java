/**
 * Provides the handlers for custom tags in the GamesCrafters Tag Library.
 * These tags are used in JSPs that are displayed to the player.
 * <p>
 * It is often cleaner to separate logic from JSPs; the tag handlers encompass
 * the logic and the JSPs use the tags to display content.
 * </p>
 */
package edu.berkeley.gcweb.servlet.gctl;